import { EventInstantForm } from 'components';

const EventsHome = () => (
  <main>
    <h1>Events!</h1>
    <section>
      <h3>Create an Event</h3>
      <EventInstantForm />
    </section>
  </main>
);

export default EventsHome;